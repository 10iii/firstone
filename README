##First One.
###Add two.
    if (err) throw err;
    
    var acdata = JSON.parse(data.toString());
    var arr = acdata.log;
    
    var i;
    for (i=0; i<arr.length; i++){
    	console.log(arr[i][0]+","+arr[i][1]+","+arr[i][2]+","+arr[i][3]+"\n");
    }
*Something.*
+Test three.
+Then 4.
